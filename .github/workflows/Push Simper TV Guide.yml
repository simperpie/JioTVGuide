name: Push Simper TV Guide

on:
  workflow_run:
    workflows: ["Simper TV Guide"]
    types:
      - completed
  workflow_dispatch:
  
jobs:
  copy-file:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Pushes test file
      uses: dmnemec/copy_file_to_another_repo_action@main
      env:
        API_TOKEN_GITHUB: ${{ secrets.Push_File }}
      with:
        source_file: 'tempest_config/epg/simper_epg.xml.gz'
        destination_repo: 'simperpie/simperpie.github.io'
        destination_folder: 'TVGuide'
        user_email: '91105635+simperpie@users.noreply.github.com'
        user_name: 'simperpie'
        commit_message: 'Automated TVGuide'
        
